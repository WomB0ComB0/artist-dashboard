const url = '<url>';
const name = '<name>';
const email = '<email>';
const des = '<description>';

export const app: {
  name: string;
  url: string;
  email: string;
  description?: string;
} = {
  name: name,
  url: url,
  email: email,
  description: des,
};
