// import { app } from '@/constants';
// export const getApiUrl = (): string => {
//   const environment = process.env.NODE_ENV;
//   let apiUrl = '';

//   environment === 'development' ? (apiUrl = 'http://localhost:3000/') : (apiUrl = `${app.url}`);

//   return apiUrl;
// };
